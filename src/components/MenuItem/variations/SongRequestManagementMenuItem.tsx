import { QueueMusic } from "@mui/icons-material";
import { MenuItem } from "..";

export const SongRequestManagementMenuItem = () => {
  return <MenuItem icon={<QueueMusic />}>신청곡 대기열 관리</MenuItem>;
};
