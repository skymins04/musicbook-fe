export const DOMAIN = process.env.DOMAIN || "musicbook.dev";
export const DOT_DOMAIN = `.${DOMAIN}`;
export const HOST = `https://${DOMAIN}`;
export const API_HOST = `https://api.${DOMAIN}`;
export const ASSET_HOST = `https://assets.${DOMAIN}`;
